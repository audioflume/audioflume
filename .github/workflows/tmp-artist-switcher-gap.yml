name: Temporary artist switcher gap

on:
  push:
    branches:
      - style/artist-switcher-profile-gap

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: style/artist-switcher-profile-gap
      - name: Update artist switcher profile gap
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('apps/web/components/artists/ArtistDashboardShell.tsx')
          text = path.read_text()
          old = 'flex max-h-64 flex-col gap-px overflow-y-auto'
          new = 'flex max-h-64 flex-col gap-1.5 overflow-y-auto'
          if text.count(old) != 1:
              raise SystemExit(f'Expected exactly one match, found {text.count(old)}')
          path.write_text(text.replace(old, new))
          PY
      - name: Commit change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/components/artists/ArtistDashboardShell.tsx
          git commit -m "Match artist switcher profile gap to padding"
          git push origin HEAD:style/artist-switcher-profile-gap
