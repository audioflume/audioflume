import type { ReactNode } from "react";

type DiscoverTemplateProps = {
  children: ReactNode;
};

export default function DiscoverTemplate({ children }: DiscoverTemplateProps) {
  return children;
}
